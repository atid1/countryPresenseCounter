export default function Home() {
  return (
    <main>
      <h1>Days per Country</h1>
      <p>Minimal starter. Add trips, import CSV, export CSV. Metrics view exists as SQL view (query it in your API or build an Insights page).</p>
    </main>
  );
}
